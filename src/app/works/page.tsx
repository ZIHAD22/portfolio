import Main from "@/pages/Works/Main";
import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "Works | Md Zihad",
  description: "Full Stack Javascript Developer",
};

const Works = () => {
  return <Main />;
};

export default Works;
